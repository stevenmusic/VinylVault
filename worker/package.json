{
  "name": "vinylvault-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker proxy between the VinylVault frontend and Turso.",
  "scripts": {
    "dev": "wrangler dev --config ../wrangler.toml",
    "deploy": "wrangler deploy --config ../wrangler.toml",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
